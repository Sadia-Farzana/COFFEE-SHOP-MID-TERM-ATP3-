<!DOCTYPE html>
  <html>
  <head>
      <title>Delete Page</title>
      <style>
      table.center {
            margin-left: auto;
            margin-right: auto;
          }
      </style>
  </head>
  <body>
    <form method="post">
      <fieldset>Delete Profile</legend>
        <table class="center">
          <tr>
            <td> ID </td>
            <td>:<%= user.id %></td>
          </tr>
          <tr>
            <td>Name</td>
            <td><input type="text" value="<%= user.name %>"  name="name"  id="name"></td>
          </tr>
          
          <tr>
            <td>User Name</td>
            <td><input type="text" value="<%= user.username %>"  name="username"  id="username"></td>
          </tr>
          <tr>
            <td>Password</td>
            <td><input type="password" value="<%= user.password %>"name="password"  id="password"></td>
          </tr>
          <tr>
            <td>Phone</td>
            <td><input type="text" value="<%= user.phone %>"  name="phone"  id="phone"></td>
          </tr>
          <tr>
            <td>Email</td>
            <td><input type="text" value="<%= user.email %>"  name="email"  id="email"></td>
          </tr>
          
          <tr>
            <td>Address</td>
            <td><input type="text" value="<%= user.address %>" name="address"  id="address"></td>
          </tr>

            <tr>
                <td><h3>Are you sure to delete the account?</h3></td>
              </tr>
              <tr>
                <td><input type="hidden" name="id" value="<%= user.id %>"></td>
                <td><input type="submit" name="choice" value="Confirm"></td>
              </tr>
        </table>
    </fieldset>
  </form>
  <table class="center">
      <tr>
        <td><a href="/home">Back</a>|
      	<a href="/logout">Logout</a></td>
      </tr>
  </table>
</body>
</html>